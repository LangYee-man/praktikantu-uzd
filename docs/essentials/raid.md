# RAID
- Create RAID1 out of two disks. The RAID must be created on a *partition*.
- Format the partition as ext4
- Check the status of the RAID array
- Create a directory in `/mnt`
- Mount the RAID array in the created directory
