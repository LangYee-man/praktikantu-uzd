# Firewall
1. Configure the firewall to accept SSH connections only from the Office IP and your Home IP
2. Configure the firewall to accept SSH Connections only from a server via the private VLAN
3. Create a simple website and allow connections only from the Office/Home IP
4. Allow pings only from the Office/Home IP
---

Note: 
If you lock yourself out of SSH you can connect to the server via the Console.
